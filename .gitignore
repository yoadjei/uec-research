__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
/data/
/runs/
/results/
*.npz
*.parquet

# local working artefacts, not part of the deliverable
/tasks/
/explanation_drift_audit.md
/docs/phase0_verification.md

# a measured GPU result recorded verbatim; it cannot be regenerated locally
!/results/scale_text_heavy.csv
!/results/scale_text_sweep_ig.csv
!/results/scale_text_sweep_summary.csv

# Paper sources, untracked while the submission is under double-blind review. This repository is
# public and every commit carries the authors' names, so the manuscript must not sit in it: a
# reviewer searching a distinctive phrase would land here. Restore after decisions.
/paper/iclr2027/main.tex
/paper/iclr2027/main.pdf
# paper.md is the same manuscript in markdown, title and abstract verbatim. Removing only the
# LaTeX would have left the whole paper here in plain text.
/paper/paper.md
/paper/reviews.md
/paper/novelty_delta.md

# Drafting and literature-audit notes. Not needed to reproduce anything; kept locally.
# docs/spec.md, theory.md and preregistration_scale.md stay tracked: the first is the
# implementation contract, the second the derivations the theory tests check, the third the
# analysis plan fixed before the sweep ran.
/docs/audit_coverage.md
/docs/audit_inventory.md
/docs/lit_matrix.csv
/docs/terminology_map.md
/docs/reference_audit.md

# Supplementary archive and the staging tree it is built from: large, regenerated, not source.
/paper/supplementary.zip
/paper/table/
/paper/tables.md
/paper/checkpoints/
/paper/configs/
/paper/experiments/
/paper/figures/
/paper/results/

# Local working notes
/general writing style.txt
/iclr-2027-style-files/
